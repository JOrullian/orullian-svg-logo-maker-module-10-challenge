const Shapes = require('./shapes');
const ShapeColor = require('./shapeColor');
const Text = require('./text');
const TextColor = require('./textColor');

function createSvgCode(text, shape, shapeColor, textColor) {
    
}